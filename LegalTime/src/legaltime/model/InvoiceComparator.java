// ______________________________________________________
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
// - problems and suggestions to
//   http://sourceforge.net/tracker/?group_id=54687
// - feedbacks and ideas on
//   http://sourceforge.net/forum/forum.php?forum_id=182208
// ______________________________________________________

package legaltime.model;

import java.util.Comparator;


/**
 * Comparator class is used to sort the InvoiceBean objects.
 * @author sql2java
 */
public class InvoiceComparator implements Comparator
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for InvoiceComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new InvoiceComparator(InvoiceManager.ID_LAST_UPDATE, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>InvoiceManager.ID_LAST_UPDATE
     *   <li>InvoiceManager.ID_CLIENT_ID
     *   <li>InvoiceManager.ID_PREV_BALANCE_DUE
     *   <li>InvoiceManager.ID_INVOICE_TOTAL_AMT
     *   <li>InvoiceManager.ID_GENERATED_DATE
     *   <li>InvoiceManager.ID_INVOICE_DT
     *   <li>InvoiceManager.ID_INVOICE_ID
     * </ul>
     */
    public InvoiceComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for InvoiceComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new InvoiceComparator(InvoiceManager.ID_LAST_UPDATE, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>InvoiceManager.ID_LAST_UPDATE
     *   <li>InvoiceManager.ID_CLIENT_ID
     *   <li>InvoiceManager.ID_PREV_BALANCE_DUE
     *   <li>InvoiceManager.ID_INVOICE_TOTAL_AMT
     *   <li>InvoiceManager.ID_GENERATED_DATE
     *   <li>InvoiceManager.ID_INVOICE_DT
     *   <li>InvoiceManager.ID_INVOICE_ID
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public InvoiceComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        InvoiceBean b1 = (InvoiceBean)pObj1;
        InvoiceBean b2 = (InvoiceBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case InvoiceManager.ID_LAST_UPDATE:
                if (b1.getLastUpdate() == null && b2.getLastUpdate() != null) {
                    iReturn = -1;
                } else if (b1.getLastUpdate() == null && b2.getLastUpdate() == null) {
                    iReturn = 0;
                } else if (b1.getLastUpdate() != null && b2.getLastUpdate() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getLastUpdate().compareTo(b2.getLastUpdate());
                }
                break;
            case InvoiceManager.ID_CLIENT_ID:
                if (b1.getClientId() == null && b2.getClientId() != null) {
                    iReturn = -1;
                } else if (b1.getClientId() == null && b2.getClientId() == null) {
                    iReturn = 0;
                } else if (b1.getClientId() != null && b2.getClientId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getClientId().compareTo(b2.getClientId());
                }
                break;
            case InvoiceManager.ID_PREV_BALANCE_DUE:
                if (b1.getPrevBalanceDue() == null && b2.getPrevBalanceDue() != null) {
                    iReturn = -1;
                } else if (b1.getPrevBalanceDue() == null && b2.getPrevBalanceDue() == null) {
                    iReturn = 0;
                } else if (b1.getPrevBalanceDue() != null && b2.getPrevBalanceDue() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getPrevBalanceDue().compareTo(b2.getPrevBalanceDue());
                }
                break;
            case InvoiceManager.ID_INVOICE_TOTAL_AMT:
                if (b1.getInvoiceTotalAmt() == null && b2.getInvoiceTotalAmt() != null) {
                    iReturn = -1;
                } else if (b1.getInvoiceTotalAmt() == null && b2.getInvoiceTotalAmt() == null) {
                    iReturn = 0;
                } else if (b1.getInvoiceTotalAmt() != null && b2.getInvoiceTotalAmt() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getInvoiceTotalAmt().compareTo(b2.getInvoiceTotalAmt());
                }
                break;
            case InvoiceManager.ID_GENERATED_DATE:
                if (b1.getGeneratedDate() == null && b2.getGeneratedDate() != null) {
                    iReturn = -1;
                } else if (b1.getGeneratedDate() == null && b2.getGeneratedDate() == null) {
                    iReturn = 0;
                } else if (b1.getGeneratedDate() != null && b2.getGeneratedDate() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getGeneratedDate().compareTo(b2.getGeneratedDate());
                }
                break;
            case InvoiceManager.ID_INVOICE_DT:
                if (b1.getInvoiceDt() == null && b2.getInvoiceDt() != null) {
                    iReturn = -1;
                } else if (b1.getInvoiceDt() == null && b2.getInvoiceDt() == null) {
                    iReturn = 0;
                } else if (b1.getInvoiceDt() != null && b2.getInvoiceDt() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getInvoiceDt().compareTo(b2.getInvoiceDt());
                }
                break;
            case InvoiceManager.ID_INVOICE_ID:
                if (b1.getInvoiceId() == null && b2.getInvoiceId() != null) {
                    iReturn = -1;
                } else if (b1.getInvoiceId() == null && b2.getInvoiceId() == null) {
                    iReturn = 0;
                } else if (b1.getInvoiceId() != null && b2.getInvoiceId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getInvoiceId().compareTo(b2.getInvoiceId());
                }
                break;
            default:
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }}
